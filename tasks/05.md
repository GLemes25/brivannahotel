# TASK: Configurar Envio de E-mail B2B e Centralização de Dados

Preciso implementar o envio real de e-mail para o formulário `AgreementDialog.tsx` e centralizar as informações do hotel em um arquivo de configuração.

## 1. Criação do Data Center:

- Crie o arquivo `app/data/brivanna.json`.
- O conteúdo deve ser um objeto JSON com as chaves em inglês: `name`, `email` (valor: gabriellemes924@gmail.com), `phone`, `whatsappNumber`, `address`, `instagramUrl`, e `websiteUrl`.
- Adicione os dados reais do hotel que você já conhece ou use placeholders realistas para Campo Grande.

## 2. Criação da API de E-mail (Backend):

- Crie uma Route Handler em `app/api/send-agreement/route.ts`.
- Utilize a biblioteca `resend` (ou `nodemailer` se preferir, mas priorize resend) para processar o envio.
- O e-mail deve ser enviado PARA o endereço contido no `brivanna.json`.
- O corpo do e-mail deve ser formatado profissionalmente em HTML, listando: Empresa, CNPJ, E-mail do Solicitante e Volume de Estadias.

## 3. Integração no Frontend (AgreementDialog.tsx):

- No `onSubmit`, substitua o `setTimeout` por uma chamada `fetch` para a rota `/api/send-agreement`.
- Importe o e-mail de destino do `app/data/brivanna.json` se necessário, ou garanta que a API aponte para ele.
- Garanta que o estado de `loading` e `isSuccess` funcione corretamente com a resposta da API.

## 4. Refinamento de Código:

- Certifique-se de que o import do componente `Image` do Next.js foi adicionado no topo do `AgreementDialog.tsx` para o ícone do WhatsApp.
- Mantenha a tipagem TypeScript rigorosa para os dados extraídos do JSON.

Siga as diretrizes de `.claude/rules` para manter o código limpo e modular.
