# TASK: Refatorar Features para Estilo Premium Minimalista (B2B Focus)

Preciso de uma refatoração estratégica no componente `app/components/Features.tsx` para torná-lo mais compacto, "direto ao ponto" e com foco total na conversão empresarial.

## 🏗️ Reorganização de Arquivos:

1. **Extração:** Mova a seção de convênio (banner inferior) para um novo componente chamado `app/components/Agreement.tsx`.
2. **Renomeação:** Renomeie o arquivo `app/components/ConvenioDialog.tsx` para `app/components/AgreementDialog.tsx` e atualize todos os imports.

## 🎨 Ajustes de UI/UX (Estilo Seção Única):

1. **Altura e Espaçamento:** Reduza o `py-12` para algo que permita visualizar a seção inteira em uma única rolagem (viewport height amigável). Minimize os `space-y` entre os blocos, mas mantenha o respiro visual.
2. **Legibilidade Inclusiva:** Aumente o `font-size` dos parágrafos de `text-base` para `text-lg` e use `leading-relaxed`. Para os títulos, mantenha a Serif, mas use `tracking-tight` para um visual mais sofisticado. O objetivo é ser legível para idosos sem perder o refinamento.
3. **Bento Grid Refinado:** Diminua os 4 cards de benefícios. Eles devem ser discretos, quase como selos de qualidade, para que o destaque real seja a chamada corporativa.

## 🤝 Novo Componente `Agreement.tsx` (Destaque Premium):

- **Visual:** Fuja do "box com borda" padrão. Use um fundo `bg-primary/5` (Ouro Brivanna muito sutil) com uma borda lateral ou um detalhe minimalista que indique exclusividade.
- **Minimalismo:** Remova sombras pesadas. Use o ícone `Handshake` de forma elegante, talvez com um efeito de "glow" suave.
- **Chamada:** O foco deve ser o benefício imediato: "Tarifas diferenciadas e faturamento direto".
- **Interatividade:** O botão deve ser o elemento de maior contraste da página.

## 📩 Fluxo de Formulário no `AgreementDialog.tsx`:

- **Workflow:** Implemente a lógica de troca de estado no Dialog.
  - Estado 1: Formulário (Empresa, CNPJ, Email, Estadias).
  - Estado 2: Sucesso (Fundo branco, ícone minimalista de check).
  - **Destaque:** No estado de sucesso, adicione um botão de "Atendimento Prioritário via WhatsApp" que direcione para o comercial.

## 🛠️ Especificações Técnicas:

- Mantenha `framer-motion` para animações de entrada.
- Use estritamente as variáveis de cor `-primary` do `globals.css`.
- Garanta que os componentes do Shadcn/UI estejam apontando para `@/components/ui`.

Refatore o código para ser limpo, sem redundâncias e focado em uma experiência B2B de alto nível.
